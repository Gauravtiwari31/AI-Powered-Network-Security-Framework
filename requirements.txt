# Core -- required on every platform
scapy>=2.5.0

# Machine learning layer.
# Without these the engine still runs; the ML layer disables itself.
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# --- Platform-specific packet interception -------------------------------
# Installed automatically by the markers below. Neither package builds on the
# other platform, which is why the old flat requirements file could not be
# installed on Windows at all.

# Linux: NetfilterQueue needs libnetfilter-queue-dev and a C toolchain:
#   sudo apt install build-essential python3-dev libnetfilter-queue-dev
NetfilterQueue>=1.1.0; sys_platform == "linux"

# Windows: pydivert ships the WinDivert driver; requires Administrator.
pydivert>=2.1.0; sys_platform == "win32"

# --- Development ----------------------------------------------------------
# pip install -r requirements.txt pytest
# pytest>=7.4.0
